<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="BoardMapper">

  <resultMap type="Board" id="BoardMap">
    <id column="board_id" property="no"/>
    <result column="title" property="title"/>
    <result column="contents" property="content"/>
    <result column="created_date" property="registeredDate"/>
    <result column="view_count" property="viewcount"/>
  </resultMap>
  
  <!-- SQL 문에 값 삽입하기
       => #{프로퍼티명} : 파라미터 타입에 지정된 객체의 프로퍼티 명 
       => #{아무이름} : primitive/String/wrapper class인 경우 아무 이름이나.
       => #{Key} : Map객체인 경우
       => SQL 실행할 때 파라미터로 넘어오는 값의 타입을 지정한다.
          parameterType="값이들어있는객체의타입명 또는 별명"
       => Parameter 타입
       int -> _int
       Integer -> int
       String -> string
       Map -> map
       HashMao -> hashmap
       Board -> board 등
   -->
  <select id="selectBoard1" 
          resultMap="BoardMap" parameterType="int">
    select 
      board_id,
      title, 
      contents, 
      created_date,
      view_count
    from x_board
    where board_id > #{what}
  </select>
  
  <select id="selectBoard2" 
          resultMap="BoardMap" parameterType="string">
    select 
      board_id,
      title, 
      contents, 
      created_date,
      view_count
    from x_board
    where title like #{what}
  </select>
  
  <select id="selectBoard3" 
          resultMap="BoardMap" parameterType="map">
    select 
      board_id,
      title, 
      contents, 
      created_date,
      view_count
    from x_board
    limit #{startIndex}, #{size} 
  </select>
  
</mapper>







